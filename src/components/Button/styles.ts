import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#2c46b1',
        height: 48,
        width: '100%',
        alignItems: 'center',
        padding: 10,
        borderRadius: 8,
    },
    title: {
        color: '#ffffff',
        fontSize: 14,
        fontWeight: '600',
    },
}); 